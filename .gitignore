# Fonts — embedded as base64 in HTML, not needed in repo
*.otf
*.ttf
*.woff
*.woff2

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Claude Code — per-user permissions (keep launch.json shared)
.claude/settings.local.json

# Secrets
.env
.env.local
.env.*.local
.vercel
.env*.local
